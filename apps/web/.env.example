# Rename this file to .env.local and fill in your Supabase project values
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
# Optionally configure API base for the frontend service layer
# NEXT_PUBLIC_API_BASE=/api/v1

# Optional: E2E real login credentials (used by Playwright tests)
# E2E_SUPABASE_EMAIL=
# E2E_SUPABASE_PASSWORD=

# Optional: Override Playwright baseURL (CI or preview deployments)
# E2E_BASE_URL=

# Optional: Test bypass for protected routes (development only; DO NOT enable in production)
# E2E_AUTH_BYPASS=0
